package calendar

const (
	IDCalendarSwipe   = "calendar-swipe"
	IDCalendarPrev    = "calendar-prev"
	IDCalendarCurrent = "calendar-current"
	IDCalendarNext    = "calendar-next"
)

// Swipe container holds three grids side by side and will simulate an infinite
// swipe, Jan to Dec, next Jear starting from Jan again.
templ Swipe(gridPrev, gridCurrent, gridNext templ.Component) {
	// TODO: Align grids properly, (CSS) current grid is visible, prev grid is offscreen (left) and next grid is offscreen (right)
	<div
		id={ IDCalendarSwipe }
		onpointermove="onPointerMove(event)"
		onpointerdown="onPointerDown(event)"
		onpointerup="onPointerUp(event)"
		onpointercancel="onPointerCancel(event)"
	>
		<div id={ IDCalendarPrev } class="grid-container">
			@gridPrev
		</div>
		<div id={ IDCalendarCurrent } class="grid-container">
			@gridCurrent
		</div>
		<div id={ IDCalendarNext } class="grid-container">
			@gridNext
		</div>
	</div>
}
