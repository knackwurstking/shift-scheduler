export default class SwipeHandler {
    /** @type {HTMLElement} */
    #container

    /**
     * @param {HTMLElement} container
     */
    constructor(container) {
        this.#container = container
    }

    start() {
        // TODO: Start animation frame handler
    }

    stop() {
        // TODO: Kill the animation frame handler
    }
}
